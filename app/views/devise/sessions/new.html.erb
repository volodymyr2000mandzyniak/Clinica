<div class="row align-items-center g-lg-5 py-5">
  <div class="col-lg-7 text-center text-lg-start">
    <h1 class="display-4 fw-bold lh-1 mb-3">Welcome to Our Clinic</h1>
    <p class="col-lg-10 fs-4">Discover the excellence of our clinic with our vertically centered hero sign-up form. Our clinic is committed to providing exceptional healthcare services. The example form below is built entirely with Bootstrap’s form controls, ensuring a seamless experience for our valued patients. Each required form group has a validation state that can be triggered by attempting to submit the form without completing it.</p>
  </div>
  <div class="col-md-10 mx-auto col-lg-5">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "p-4 p-md-5 border rounded-3 bg-light") do |f| %>
      <div class="form-floating mb-3">
        <%= f.text_field :phone, autofocus: true, class: "form-control", placeholder: "096 00440099" %>
        <label for="floatingInput">Phone number</label>
      </div>
      <div class="form-floating mb-3">
        <%= f.password_field :password, class: "form-control", id: "floatingPassword", placeholder: "Password" %>
        <label for="floatingPassword">Password</label>
      </div>
      <% if devise_mapping.rememberable? %>
        <div class="checkbox mb-3">
          <label>
            <%= f.check_box :remember_me %>
            Remember me
          </label>
        </div>
      <% end %>
      <%= f.button "Sign up", class: "w-100 btn btn-lg btn-primary", type: "submit" %>
      <hr class="my-4">
      <%= render "devise/shared/links" %>

      <small class="text-muted">By clicking Sign up, you agree to the terms of use.</small>
    <% end %>
  </div>
</div>



